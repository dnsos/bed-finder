<%= turbo_stream.update 'new-occupancy' do %>
  <%= link_to 'Neue Belegung', new_facility_occupancy_path(@facility), class: 'bg-black text-white border p-2' %>
<% end %>

<%= turbo_stream.prepend 'occupancies', partial: 'occupancies/occupancy', locals: { occupancy: @occupancy } %>

<%= turbo_stream.update "#{dom_id(@facility)}_availabilities" do %>
  <%= render Facility::AvailabilityStatsComponent.new(label: "Freie Betten (#{@beds_count} insgesamt)") do |component| %>
    <%= component.with_availability_stat(label: 'Für Männer', number: @available_beds_men.count - @men_to_deduct) %>
    <%= component.with_availability_stat(label: 'Für Frauen', number: @available_beds_women.count - @women_to_deduct) %>
    <%= component.with_availability_stat(label: 'Für Diverse', number: @available_beds_diverse.count - @diverse_to_deduct) %>
  <% end %>
<% end %>